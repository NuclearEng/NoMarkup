apiVersion: v1
kind: Service
metadata:
  name: bidding
  labels:
    app.kubernetes.io/name: bidding
    app.kubernetes.io/component: rust-engine
    app.kubernetes.io/part-of: nomarkup
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: bidding
  ports:
    - name: grpc
      port: 50053
      targetPort: grpc
      protocol: TCP
