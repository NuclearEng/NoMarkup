apiVersion: v1
kind: Service
metadata:
  name: imaging
  labels:
    app.kubernetes.io/name: imaging
    app.kubernetes.io/component: rust-engine
    app.kubernetes.io/part-of: nomarkup
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: imaging
  ports:
    - name: grpc
      port: 50058
      targetPort: grpc
      protocol: TCP
