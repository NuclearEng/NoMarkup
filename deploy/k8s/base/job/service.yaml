apiVersion: v1
kind: Service
metadata:
  name: job
  labels:
    app.kubernetes.io/name: job
    app.kubernetes.io/component: go-service
    app.kubernetes.io/part-of: nomarkup
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: job
  ports:
    - name: grpc
      port: 50052
      targetPort: grpc
      protocol: TCP
