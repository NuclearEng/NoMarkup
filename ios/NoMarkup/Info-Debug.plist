<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleDevelopmentRegion</key>
	<string>$(DEVELOPMENT_LANGUAGE)</string>
	<key>CFBundleDisplayName</key>
	<string>NoMarkup</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleShortVersionString</key>
	<string>$(MARKETING_VERSION)</string>
	<key>CFBundleVersion</key>
	<string>$(CURRENT_PROJECT_VERSION)</string>
	<key>LSRequiresIPhoneOS</key>
	<true/>
	<key>UIApplicationSceneManifest</key>
	<dict>
		<key>UIApplicationSupportsMultipleScenes</key>
		<true/>
	</dict>
	<key>UILaunchScreen</key>
	<dict>
		<key>UIColorName</key>
		<string>AccentColor</string>
	</dict>
	<key>UISupportedInterfaceOrientations</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
	<key>UISupportedInterfaceOrientations~ipad</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationPortraitUpsideDown</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
	<key>UIRequiresFullScreen</key>
	<false/>
	<!--
	  No UIBackgroundModes / remote-notification: alert pushes do not require background mode.
	  Only re-add remote-notification if content-available silent push is implemented (MISC.3).
	  Device register → POST /api/v1/notifications/devices still works without this mode.
	-->
	<!--
	  API base URL override. Leave empty so AppConfig defaults apply
	  (simulator loopback / stamped DevAPIBase LAN / production HTTPS).
	  Never commit a cleartext LAN URL or secrets here. Device Debug LAN
	  is AppConfig + NSAllowsLocalNetworking below, not this key.
	-->
	<key>APIBaseURL</key>
	<string></string>
	<!--
	  DEBUG-ONLY plist (target INFOPLIST_FILE). Release ships Info.plist with
	  default ATS (no NSAppTransportSecurity). Local-networking exception lets
	  a physical device load http://192.168.x.x:8081. No ArbitraryLoads.
	-->
	<key>NSAppTransportSecurity</key>
	<dict>
		<key>NSAllowsLocalNetworking</key>
		<true/>
	</dict>
	<!-- Stripe publishable key (pk_test_… / pk_live_…). Override with NOMARKUP_STRIPE_PUBLISHABLE_KEY. -->
	<key>StripePublishableKey</key>
	<string></string>
	<!-- Apple Pay merchant ID — register in Apple Developer + Stripe Dashboard. -->
	<key>ApplePayMerchantId</key>
	<string>merchant.com.nomarkup.app</string>
	<!--
	  Google Sign-In (FR-1.1): iOS OAuth client ID from Google Cloud Console.
	  Also add the reverse-client-id URL scheme under CFBundleURLTypes, e.g.
	  client 123-abc.apps.googleusercontent.com → scheme com.googleusercontent.apps.123-abc
	  Mirror the same value as gateway GOOGLE_IOS_CLIENT_ID (id_token aud).
	  Override: NOMARKUP_GOOGLE_IOS_CLIENT_ID.
	-->
	<key>GoogleIosClientID</key>
	<string></string>
	<!--
	  Facebook Login (FR-1.1): Meta App ID (public; same as gateway FACEBOOK_CLIENT_ID).
	  Register Valid OAuth Redirect URI: nomarkup://oauth2redirect/facebook
	  Override: NOMARKUP_FACEBOOK_APP_ID. Client secret stays on the gateway only.
	-->
	<key>FacebookAppID</key>
	<string></string>
	<!--
	  Location: single When-In-Use key covers market picker + job-site check-in.
	  Check-in-only draft (inventory): "NoMarkup uses your location to confirm you arrived at the job site. Check-in location is stored with the contract for dispute protection."
	  Market-only draft: "NoMarkup uses your location to suggest the nearest marketplace city. You can always pick a city manually."
	-->
	<key>NSLocationWhenInUseUsageDescription</key>
	<string>NoMarkup uses your location to suggest the nearest marketplace city and, when you check in to a job, to confirm you arrived at the job site for dispute protection. You can pick a city manually.</string>
	<key>NSPhotoLibraryUsageDescription</key>
	<string>NoMarkup needs access to your photos so you can set a profile picture, add portfolio images, and attach photos to jobs, listings, and claims.</string>
	<key>NSCameraUsageDescription</key>
	<string>NoMarkup uses the camera so you can take photos for jobs, listings, or your profile instead of choosing an existing photo.</string>
	<!-- Face ID for optional app lock + sensitive actions (IOS-SEC.7). -->
	<key>NSFaceIDUsageDescription</key>
	<string>NoMarkup uses Face ID to protect sensitive actions like account deletion and removing payment methods, and to unlock the app when you enable app lock.</string>
	<!-- Live Activities for auction countdowns after you place a bid (IOS-SYS.LA.4). -->
	<key>NSSupportsLiveActivities</key>
	<true/>
	<!-- Do NOT add NSMicrophoneUsageDescription or NSUserTrackingUsageDescription — unused. -->
	<key>CFBundleURLTypes</key>
	<array>
		<dict>
			<key>CFBundleURLName</key>
			<string>com.nomarkup.app</string>
			<key>CFBundleURLSchemes</key>
			<array>
				<string>nomarkup</string>
			</array>
		</dict>
	</array>
	<!-- Export compliance: only exempt encryption (HTTPS / OS crypto). -->
	<!--
	  StoreKit / Rail B digital subscriptions (FR-12). Default OFF for App Store 3.1.1
	  free-tier binary. Flip to true only after ASC products + Review Notes.
	  Override: NOMARKUP_STOREKIT_ENABLED=true
	-->
	<key>StoreKitEnabled</key>
	<false/>
	<!-- Comma-separated ASC product IDs (or use string array). Defaults match AppConfig.defaultStoreKitProductIDs. -->
	<key>StoreKitProductIDs</key>
	<string>nomarkup.provider.pro.monthly,nomarkup.provider.pro.yearly,nomarkup.provider.business.monthly,nomarkup.provider.business.yearly</string>
	<key>ITSAppUsesNonExemptEncryption</key>
	<false/>
	<!-- FR-10.4: allow canOpenURL for Google Maps app scheme (optional directions). -->
	<key>LSApplicationQueriesSchemes</key>
	<array>
		<string>comgooglemaps</string>
	</array>
</dict>
</plist>
