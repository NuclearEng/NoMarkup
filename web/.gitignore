tsconfig.tsbuildinfo

# Playwright build artifacts (generated by `playwright test --reporter=html`
# or `npx playwright show-report`). Never commit — they're large + ephemeral.
playwright-report/
test-results/

# Lighthouse CI reports / runtime (PERF-02; generated by npm run lighthouse:ci)
lighthouse-reports/
.lighthouseci/
lighthouserc.runtime.cjs
.next-lhci/
